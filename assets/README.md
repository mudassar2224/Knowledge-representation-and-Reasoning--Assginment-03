# Assets

Put your profile image here so the Streamlit UI can use it as the assistant avatar.

Recommended filename:

- `profile.png`

Also supported:

- `profile.jpg`
- `profile.jpeg`
- `profile.webp`

If no image is found, the app falls back to an emoji avatar.